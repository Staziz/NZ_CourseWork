# ResNet18 API for Garbage Classification

# установка и запуск

1. тебе нужен Python 3.10+
2. установи зависимости(кажется я там всё указал): pip install -r requirements.txt
3. из директории проекта запускаешь: python3 app.py (получается запустили api)
4. test_request.py - это пример запроса на питоне, можешь запустить он сразу выдаёт результат 
5. если я всё правильно понимаю ты пишешь свой POST-запрос за C# и получаешь такой же результат

